export enum DynamicFormValidatorNameEnum {
    custom = 'custom',
    email = 'email',
    min = 'min',
    minLength = 'minLength',
    max = 'max',
    maxLength = 'maxLength',
    pattern = 'pattern',
    required = 'required',
}
