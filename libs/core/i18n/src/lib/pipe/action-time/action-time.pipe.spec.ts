import { ActionTimePipe } from './action-time.pipe';

describe('ActionTimePipe', () => {
    it('create an instance', () => {
        const pipe = new ActionTimePipe();

        expect(pipe).toBeTruthy();
    });
});
