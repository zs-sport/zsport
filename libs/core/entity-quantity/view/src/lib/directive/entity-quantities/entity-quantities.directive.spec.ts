import { EntityQuantitiesDirective } from './entity-quantities.directive';

describe('EntityQuantitiesDirective', () => {
    it('should create an instance', () => {
        const directive = new EntityQuantitiesDirective();
        expect(directive).toBeTruthy();
    });
});
